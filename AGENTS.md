## Local Effect Source

The Effect repository is using a new beta version cloned to `~/.local/share/effect-4.0` for reference.
Use this to explore APIs, find usage examples, and understand implementation
details when the documentation isn't enough.

## Formatting and Linting

Always format and lint your code with `pnpm fmt` and `pnpm lint:fix` in the correct project.
